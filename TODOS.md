# TODOS

- [x] Create a `/logprompt` slash command that appends the current prompt to PromptHistory.md in the current directory (usually a git repo root). Format should match the existing PromptHistory.md format.
- [x] Add a copy of the `/logprompt` slash command code to the kenyaccounting repo if not already the standard location.
- [x] Add paystub generation to the kenyaccounting software core scope if not already included.
- [x] Create a spec for unit tests covering the software's stated goals.
